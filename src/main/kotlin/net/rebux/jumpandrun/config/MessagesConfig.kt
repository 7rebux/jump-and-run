package net.rebux.jumpandrun.config

/**
 * We only store messages inside this config which can be seen by players
 * without any special permissions.
 */
object MessagesConfig : CustomConfiguration("messages.yml")
